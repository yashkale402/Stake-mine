# Backend host used by Next.js rewrites (no trailing slash, no path)
# In Docker Compose this should be http://backend:3001
# For local dev leave as default below
NEXT_PUBLIC_API_URL=http://localhost:3001
